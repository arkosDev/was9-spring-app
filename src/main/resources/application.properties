# ==========================================
# API Backend - WAS9 / Spring / Hibernate / JDK 8
# ==========================================

# Hibernate - cambiar dialect segun BD del proyecto
hibernate.dialect=org.hibernate.dialect.DB2Dialect
hibernate.show_sql=false
hibernate.hbm2ddl.auto=validate

# JWT
jwt.secret=CambiarEsteSecretoEnProduccion_MinLength32Chars!!
jwt.expiration.ms=86400000

# App
app.name=ApiBackend
app.version=1.0.0
