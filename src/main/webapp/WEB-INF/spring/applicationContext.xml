<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context
           http://www.springframework.org/schema/context/spring-context.xsd
           http://www.springframework.org/schema/tx
           http://www.springframework.org/schema/tx/spring-tx.xsd
           http://www.springframework.org/schema/aop
           http://www.springframework.org/schema/aop/spring-aop.xsd">

    <!-- ==========================================
         Component Scan (excluye @Controller - los maneja el dispatcher)
         ========================================== -->
    <context:component-scan base-package="com.empresa.app">
        <context:exclude-filter type="annotation"
            expression="org.springframework.stereotype.Controller"/>
    </context:component-scan>

    <!-- Habilitar anotaciones de configuracion -->
    <context:annotation-config/>

    <!-- Cargar propiedades externas -->
    <context:property-placeholder location="classpath:application.properties"/>

    <!-- ==========================================
         Data Source - usando JNDI de WAS 9
         ========================================== -->
    <bean id="dataSource" class="org.springframework.jndi.JndiObjectFactoryBean">
        <property name="jndiName" value="java:comp/env/jdbc/AppDataSource"/>
        <property name="resourceRef" value="true"/>
    </bean>

    <!-- ==========================================
         JPA EntityManagerFactory
         WAS 9 provee el proveedor JPA (OpenJPA por defecto).
         Sin Hibernate: se usa el proveedor del contenedor.
         ========================================== -->
    <bean id="entityManagerFactory"
          class="org.springframework.orm.jpa.LocalContainerEntityManagerFactoryBean">
        <property name="dataSource" ref="dataSource"/>
        <property name="packagesToScan" value="com.empresa.app.model"/>
        <!-- Sin jpaVendorAdapter: WAS 9 usa su propio proveedor JPA (IBM OpenJPA) -->
        <property name="persistenceUnitName" value="appPU"/>
        <property name="jpaProperties">
            <props>
                <prop key="openjpa.Log">DefaultLevel=WARN</prop>
            </props>
        </property>
    </bean>

    <!-- ==========================================
         Transaction Manager JPA
         ========================================== -->
    <bean id="transactionManager"
          class="org.springframework.orm.jpa.JpaTransactionManager">
        <property name="entityManagerFactory" ref="entityManagerFactory"/>
    </bean>

    <!-- Habilitar @Transactional -->
    <tx:annotation-driven transaction-manager="transactionManager"/>

    <!-- ==========================================
         AOP
         ========================================== -->
    <aop:aspectj-autoproxy/>

</beans>
