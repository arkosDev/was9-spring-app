package com.ing.ia3.mx.seguros.autos.emisionpolizas.util;
// TODO: confirmar si esta clase también se movió al paquete com.axa.seguros.autos.*

import com.axa.seguros.autos.dto.RespuEmmaPolDto;
import com.axa.seguros.autos.dto.PolizaDto;
import com.axa.seguros.autos.dto.BeneficiarioDto;
import com.axa.seguros.autos.dto.ClienteYODto;
import com.axa.seguros.autos.dto.EndososSOAPDto;
import com.axa.seguros.autos.dto.EndososBDto;
import com.axa.seguros.autos.dto.RecibosDto;
import com.axa.seguros.autos.dto.CoberturasDto;
import com.axa.seguros.autos.dto.CondRegistDto;
import com.axa.seguros.autos.dto.EquipEspDto;

import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.io.StringReader;
import java.io.StringWriter;
import java.util.Iterator;

import javax.xml.stream.XMLOutputFactory;
import javax.xml.stream.XMLStreamException;
import javax.xml.stream.XMLStreamWriter;
import javax.xml.transform.OutputKeys;
import javax.xml.transform.Transformer;
import javax.xml.transform.TransformerException;
import javax.xml.transform.TransformerFactory;
import javax.xml.transform.stream.StreamResult;
import javax.xml.transform.stream.StreamSource;

// TODO: confirmar el import estático real de las constantes (GENERALES, NUMEROPOLIZA, etc.)
import static com.ing.ia3.mx.seguros.autos.emisionpolizas.misc.ConstantesXMLSalida.*;

public class ArmaXmlPolizas {

    private static final String ENCODE = "ISO-8859-1";

    public String getXmlPoliza(final RespuEmmaPolDto respEmmDto)
        throws XMLStreamException, TransformerException, IOException {

        ByteArrayOutputStream xmlBytes = new ByteArrayOutputStream();
        XMLOutputFactory xof = XMLOutputFactory.newInstance();
        XMLStreamWriter hd = xof.createXMLStreamWriter(xmlBytes, ENCODE);

        hd.writeStartDocument(ENCODE, "1.0");
        hd.writeStartElement(POLIZA);

        addGenerales(respEmmDto, hd);
        addTitular(respEmmDto, hd);
        addCondicionesEmision(respEmmDto, hd);
        addBeneficiarioPreferente(respEmmDto, hd);
        addClienteYO(respEmmDto, hd);
        addConductoresRegistrados(respEmmDto, hd);
        addBeneficiariosAccidentes(respEmmDto, hd);
        addVehiculo(respEmmDto, hd);
        addEndosos(respEmmDto, hd);

        hd.writeEndElement(); // POLIZA
        hd.writeEndDocument();
        hd.flush();
        hd.close();

        String strXml = xmlBytes.toString(ENCODE);
        xmlBytes.close();

        Transformer transformer = TransformerFactory.newInstance().newTransformer();
        transformer.setOutputProperty(OutputKeys.INDENT, "yes");
        transformer.setOutputProperty("{http://xml.apache.org/xslt}indent-amount", "1");
        transformer.setOutputProperty(OutputKeys.ENCODING, ENCODE);
        StringWriter sw = new StringWriter();
        transformer.transform(new StreamSource(new StringReader(strXml)), new StreamResult(sw));
        strXml = sw.toString();
        return strXml;
    }

    private void addGenerales(final RespuEmmaPolDto respEmmDto, XMLStreamWriter hd)
        throws XMLStreamException {

        hd.writeStartElement(GENERALES);
        if (null != respEmmDto.getVConsPoliza() && !respEmmDto.getVConsPoliza().isEmpty()) {
            String aux = null;

            PolizaDto polizaValueObject = respEmmDto.getVConsPoliza().get(0);

            hd.writeStartElement(NUMEROPOLIZA);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getChrNumPoliza()));
            hd.writeEndElement();

            hd.writeStartElement(TIPO_POLIZA);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getChrTipoPoliza()));
            hd.writeEndElement();

            hd.writeStartElement(FECHADESDE);
            if (polizaValueObject.getDteFechaDesde() != null) {
                hd.writeCharacters(UtilServiceDate.formatDate(polizaValueObject.getDteFechaDesde()));
            } else {
                hd.writeCharacters("");
            }
            hd.writeEndElement();

            hd.writeStartElement(FECHAHASTA);
            if (polizaValueObject.getDteFechaHasta() != null) {
                hd.writeCharacters(UtilServiceDate.formatDate(polizaValueObject.getDteFechaHasta()));
            } else {
                hd.writeCharacters("");
            }
            hd.writeEndElement();

            hd.writeStartElement(FECHA_EMISION);
            if (polizaValueObject.getDteFechaEmision() != null) {
                hd.writeCharacters(UtilServiceDate.formatDate(polizaValueObject.getDteFechaEmision()));
            } else {
                hd.writeCharacters("");
            }
            hd.writeEndElement();

            hd.writeStartElement(ESTADO_POLIZA);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getChrEstadoPol()));
            hd.writeEndElement();

            hd.writeStartElement(FECHA_CANCELACION);
            if (polizaValueObject.getDteFechaCance() != null) {
                hd.writeCharacters(UtilServiceDate.formatDate(polizaValueObject.getDteFechaCance()));
            } else {
                hd.writeCharacters("");
            }
            hd.writeEndElement();

            hd.writeStartElement(SUBRAMO);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getChrSubramo()));
            hd.writeEndElement();

            hd.writeStartElement(FORMAPAGO);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getChrFormaPago()));
            hd.writeEndElement();

            hd.writeStartElement(MONEDA);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getChrMoneda()));
            hd.writeEndElement();

            hd.writeStartElement(AGENTE);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getIntAgente()));
            hd.writeEndElement();

            hd.writeStartElement(CENTRO_COSTOS);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getIntCentroCosto()));
            hd.writeEndElement();

            hd.writeStartElement(POLIZA_ANTERIOR);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getChrPolizaAnt()));
            hd.writeEndElement();

            hd.writeStartElement(POLIZA_SIGUIENTE);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getChrPolizaSig()));
            hd.writeEndElement();

            hd.writeStartElement(PAGO_ELECTRONICO);
            aux = assingSafeSring(polizaValueObject.getLgcPagoTarjeta());
            if (!aux.equals("")) {
                hd.writeCharacters(aux.equalsIgnoreCase("true") ? "SI" : "NO");
            } else {
                hd.writeCharacters(aux);
            }
            hd.writeEndElement();

            hd.writeStartElement(TARIFA_ALTA);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getChrTarifaAlta()));
            hd.writeEndElement();

            hd.writeStartElement(ARRENDADOR);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getIntArrendador()));
            hd.writeEndElement();

            hd.writeStartElement(VERSION);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getIntVersion()));
            hd.writeEndElement();

            hd.writeStartElement(DESCUENTO_POR_EXPERIENCIA);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getDcmDescExp()));
            hd.writeEndElement();

            hd.writeStartElement(DESCUENTO_POR_VOLUMEN);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getDcmDescVol()));
            hd.writeEndElement();

            hd.writeStartElement(SERVIDORA);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getChrServidora()));
            hd.writeEndElement();

            hd.writeStartElement(AFIANZADORA);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getChrAfianzadora()));
            hd.writeEndElement();

            hd.writeStartElement(INDICETARIFA);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getIntIndTar()));
            hd.writeEndElement();

            hd.writeStartElement(TARIFA);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getChrTarifa()));
            hd.writeEndElement();
        }
        hd.writeEndElement(); // GENERALES
    }

    private void addTitular(final RespuEmmaPolDto respEmmDto, XMLStreamWriter hd)
        throws XMLStreamException {

        if (null != respEmmDto.getVConsPoliza() && !respEmmDto.getVConsPoliza().isEmpty()) {

            PolizaDto polizaValueObject = respEmmDto.getVConsPoliza().get(0);
            hd.writeStartElement(TITULAR);
            hd.writeStartElement(TIPO_PERSONA_TITULAR);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getChrTipoPersona()));
            hd.writeEndElement();
            hd.writeStartElement(NOMBRE);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getChrNombreCte()));
            hd.writeEndElement();
            hd.writeStartElement(APELLIDO_PATERNO);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getChrApPaternoCte()));
            hd.writeEndElement();
            hd.writeStartElement(APELLIDO_MATERNO);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getChrApMaternoCte()));
            hd.writeEndElement();
            hd.writeStartElement(RFC);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getChrRFC()));
            hd.writeEndElement();
            hd.writeStartElement(DIRECCION);
            hd.writeStartElement(CALLE_NUMERO);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getChrDireccion()));
            hd.writeEndElement();
            hd.writeStartElement(COLONIA);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getChrColonia()));
            hd.writeEndElement();
            hd.writeStartElement(CODIGO_POSTAL);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getIntCodPostal()));
            hd.writeEndElement();
            hd.writeStartElement(ESTADO);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getChrDescEstado()));
            hd.writeEndElement();
            hd.writeStartElement(POBLACION);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getChrDescPobla()));
            hd.writeEndElement();
            hd.writeEndElement(); // DIRECCION
            hd.writeStartElement(TELEFONO);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getChrTelefono()));
            hd.writeEndElement();
            hd.writeEndElement(); // TITULAR
        }
    }

    private void addVehiculo(final RespuEmmaPolDto respEmmDto, XMLStreamWriter hd)
        throws XMLStreamException {

        if (null != respEmmDto.getVConsPoliza() && !respEmmDto.getVConsPoliza().isEmpty()) {
            PolizaDto polizaValueObject = respEmmDto.getVConsPoliza().get(0);
            hd.writeStartElement(VEHICULO);
            hd.writeStartElement(MODELO);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getIntModelo()));
            hd.writeEndElement();
            hd.writeStartElement(PAQUETE);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getChrPaqCont()));
            hd.writeEndElement();
            hd.writeStartElement(MARCA);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getChrMarca()));
            hd.writeEndElement();
            hd.writeStartElement(DESCRIPCION);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getChrDescVeh()));
            hd.writeEndElement();
            hd.writeStartElement(OCUPANTES);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getIntOcupantes()));
            hd.writeEndElement();
            hd.writeStartElement(MOTOR);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getChrMotor()));
            hd.writeEndElement();
            hd.writeStartElement(SERIE);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getChrSerieVeh()));
            hd.writeEndElement();
            hd.writeStartElement(PLACAS);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getChrPlacas()));
            hd.writeEndElement();
            hd.writeStartElement(ESTADO_PLACAS);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getChrEdoPlacas()));
            hd.writeEndElement();
            hd.writeStartElement(CLAVE_USO);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getIntUso()));
            hd.writeEndElement();
            hd.writeStartElement(USO);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getChrDescUso()));
            hd.writeEndElement();
            hd.writeStartElement(SERVICIO);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getChrServicio()));
            hd.writeEndElement();
            hd.writeStartElement(TIPO_CARGA);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getChrTipoCarga()));
            hd.writeEndElement();
            hd.writeStartElement(PORCENTAJE_SUMA);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getDcmPorcSuma()));
            hd.writeEndElement();
            hd.writeStartElement(VALOR);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getIntValorVeh()));
            hd.writeEndElement();
            hd.writeStartElement(CLAVE_AMIS);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getChrClaveAmis()));
            hd.writeEndElement();
            hd.writeEndElement(); // VEHICULO
        }
    }

    private void addCondicionesEmision(final RespuEmmaPolDto respEmmDto, XMLStreamWriter hd)
        throws XMLStreamException {

        if (null != respEmmDto.getVConsPoliza() && !respEmmDto.getVConsPoliza().isEmpty()) {
            String aux = null;

            PolizaDto polizaValueObject = respEmmDto.getVConsPoliza().get(0);
            hd.writeStartElement(CONDICIONESEMISION);
            hd.writeStartElement(ESVALORCOMERCIAL);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getChrValorCom()));
            hd.writeEndElement();
            hd.writeStartElement(DANOS_MATERIALES);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getIntDedDM()));
            hd.writeEndElement();
            hd.writeStartElement(ROBO_TOTAL);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getIntDedRB()));
            hd.writeEndElement();
            hd.writeStartElement(RESPONSABILIDAD_CIVIL);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getIntDedRC()));
            hd.writeEndElement();
            hd.writeStartElement(RCBIENES);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getIntSumaRC()));
            hd.writeEndElement();
            hd.writeStartElement(RCPERSONAS);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getIntSumaRCP()));
            hd.writeEndElement();
            hd.writeStartElement(ACCIDENTES);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getIntSumaAcc()));
            hd.writeEndElement();
            hd.writeStartElement(AUTO_SUSTITUTO);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getIntSumaAutoSust()));
            hd.writeEndElement();
            hd.writeStartElement(GASTOS_MEDICOS);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getIntSumaGtosMed()));
            hd.writeEndElement();
            hd.writeStartElement(SERVICIO_ASISTENCIA);
            aux = assingSafeSring(polizaValueObject.getLgcFianza());
            if (!aux.equals("")) {
                hd.writeCharacters(aux.equalsIgnoreCase("true") ? "SI" : "NO");
            } else {
                hd.writeCharacters(aux);
            }
            hd.writeEndElement();
            hd.writeStartElement(SIGUE_CON_AUTO);
            aux = assingSafeSring(polizaValueObject.getLgcSigueConAuto());
            if (!aux.equals("")) {
                hd.writeCharacters(aux.equalsIgnoreCase("true") ? "SI" : "NO");
            } else {
                hd.writeCharacters(aux);
            }
            hd.writeEndElement();
            hd.writeStartElement(FIANZA);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getIntSumaFianza()));
            hd.writeEndElement();
            hd.writeStartElement(EQUIPO_ESPECIAL);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getIntSumaEqEsp()));
            hd.writeEndElement();
            hd.writeStartElement(ADAPTACIONES);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getIntSumaAdapta()));
            hd.writeEndElement();
            hd.writeStartElement(ROBO_EXT);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getIntSumaRoboExt()));
            hd.writeEndElement();
            hd.writeStartElement(ROBO_INT);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getIntSumaRoboInt()));
            hd.writeEndElement();
            hd.writeStartElement(VIAJERO);
            hd.writeCharacters(assingSafeSring(polizaValueObject.getIntSumaViaj()));
            hd.writeEndElement();
            hd.writeStartElement(DOBLEREMOLQUE);
            aux = assingSafeSring(polizaValueObject.getLgcDobleRemol());
            if (!aux.equals("")) {
                hd.writeCharacters(aux.equalsIgnoreCase("true") ? "SI" : "NO");
            } else {
                hd.writeCharacters(aux);
            }
            hd.writeEndElement();
            hd.writeStartElement(AMPLIACION_RC);
            aux = assingSafeSring(polizaValueObject.getLgcAmplRC());
            if (!aux.equals("")) {
                hd.writeCharacters(aux.equalsIgnoreCase("true") ? "SI" : "NO");
            } else {
                hd.writeCharacters(aux);
            }
            hd.writeEndElement();
            hd.writeStartElement(EXENCION_DEDUCIBLE);
            aux = assingSafeSring(polizaValueObject.getLgcPrix());
            if (!aux.equals("")) {
                hd.writeCharacters(aux.equalsIgnoreCase("true") ? "SI" : "NO");
            } else {
                hd.writeCharacters(aux);
            }
            hd.writeEndElement();
            hd.writeStartElement(EXTENSIONRC);
            aux = assingSafeSring(polizaValueObject.getLgcExtRC());
            if (!aux.equals("")) {
                hd.writeCharacters(aux.equalsIgnoreCase("true") ? "SI" : "NO");
            } else {
                hd.writeCharacters(aux);
            }
            hd.writeEndElement();
            hd.writeStartElement(SUVA);
            aux = assingSafeSring(polizaValueObject.getLgcSUVA());
            if (!aux.equals("")) {
                hd.writeCharacters(aux.equalsIgnoreCase("true") ? "SI" : "NO");
            } else {
                hd.writeCharacters(aux);
            }
            hd.writeEndElement();

            hd.writeEndElement(); // CONDICIONESEMISION
        }
    }

    private void addClienteYO(final RespuEmmaPolDto respEmmDto, XMLStreamWriter hd)
        throws XMLStreamException {

        if (null != respEmmDto.getVConsClienteYO() && !respEmmDto.getVConsClienteYO().isEmpty()) {
            Iterator<ClienteYODto> iterClienteYO = respEmmDto.getVConsClienteYO().iterator();
            while (iterClienteYO.hasNext()) {
                ClienteYODto clienteYOValueObject = iterClienteYO.next();
                hd.writeStartElement(CLIENTEYO);

                hd.writeStartElement(NUMEROPOLIZA);
                hd.writeCharacters(assingSafeSring(clienteYOValueObject.getChrNumPoliza()));
                hd.writeEndElement();

                hd.writeStartElement(TIPO_PERSONA);
                hd.writeCharacters(assingSafeSring(clienteYOValueObject.getChrTipoPers()).toUpperCase());
                hd.writeEndElement();

                hd.writeStartElement(NOMBRE);
                hd.writeCharacters(assingSafeSring(clienteYOValueObject.getChrNombre()));
                hd.writeEndElement();

                hd.writeStartElement(APELLIDO_PATERNO);
                hd.writeCharacters(assingSafeSring(clienteYOValueObject.getChrApPaterno()));
                hd.writeEndElement();

                hd.writeStartElement(APELLIDO_MATERNO);
                hd.writeCharacters(assingSafeSring(clienteYOValueObject.getChrApMaterno()));
                hd.writeEndElement();

                hd.writeEndElement(); // CLIENTEYO
            }
        } else {
            hd.writeStartElement(CLIENTEYO);
            hd.writeEndElement();
        }
    }

    private void addConductoresRegistrados(final RespuEmmaPolDto respEmmDto, XMLStreamWriter hd)
        throws XMLStreamException {

        hd.writeStartElement(CONDUCTORES_REGISTRADOS);
        if (null != respEmmDto.getVConsConductores() && !respEmmDto.getVConsConductores().isEmpty()) {
            for (Iterator<CondRegistDto> iter = respEmmDto.getVConsConductores().iterator(); iter.hasNext();) {
                CondRegistDto conductorRegistradoVO = iter.next();
                hd.writeStartElement(CONDUCTOR);
                hd.writeStartElement(NUMERO_POLIZA_COND);
                hd.writeCharacters(assingSafeSring(conductorRegistradoVO.getStrNumPoliza()));
                hd.writeEndElement();
                hd.writeStartElement(NUMERO_INCISO);
                hd.writeCharacters(assingSafeSring(conductorRegistradoVO.getIntNumInc()));
                hd.writeEndElement();
                hd.writeStartElement(CONSECUTIVO_CONDUCTOR);
                hd.writeCharacters(assingSafeSring(conductorRegistradoVO.getIntSecCond()));
                hd.writeEndElement();
                hd.writeStartElement(NOMBRE);
                hd.writeCharacters(assingSafeSring(conductorRegistradoVO.getStrNombre()));
                hd.writeEndElement();
                hd.writeStartElement(APELLIDO_PATERNO);
                hd.writeCharacters(assingSafeSring(conductorRegistradoVO.getStrApPaterno()));
                hd.writeEndElement();
                hd.writeStartElement(APELLIDO_MATERNO);
                hd.writeCharacters(assingSafeSring(conductorRegistradoVO.getStrApMaterno()));
                hd.writeEndElement();
                hd.writeStartElement(SEXO);
                hd.writeCharacters(assingSafeSring(conductorRegistradoVO.getStrSexo()));
                hd.writeEndElement();
                hd.writeStartElement(FECHA_NACIMIENTO);
                if (conductorRegistradoVO.getDteFechaNacimiento() != null) {
                    hd.writeCharacters(UtilServiceDate.formatDate(conductorRegistradoVO.getDteFechaNacimiento()));
                } else {
                    hd.writeCharacters("");
                }
                hd.writeEndElement();
                hd.writeStartElement(DET_PRIMA);
                if (!"".equals(assingSafeSring(conductorRegistradoVO.getLgcAlta()))) {
                    hd.writeCharacters(
                        conductorRegistradoVO.getLgcAlta().toString().equalsIgnoreCase("true") ? "SI" : "NO");
                } else {
                    hd.writeCharacters("");
                }
                hd.writeEndElement();
                hd.writeStartElement(EXCLUIDO);
                if (!"".equals(assingSafeSring(conductorRegistradoVO.getLgcExcluye()))) {
                    hd.writeCharacters(
                        conductorRegistradoVO.getLgcExcluye().toString().equalsIgnoreCase("true") ? "SI" : "NO");
                } else {
                    hd.writeCharacters("");
                }
                hd.writeEndElement();
                hd.writeStartElement(FECHA_BAJA);
                if (conductorRegistradoVO.getDteFechaBaja() != null) {
                    hd.writeCharacters(UtilServiceDate.formatDate(conductorRegistradoVO.getDteFechaBaja()));
                } else {
                    hd.writeCharacters("");
                }
                hd.writeEndElement();
                hd.writeStartElement(ES_CONTRATANTE);
                if (!"".equals(assingSafeSring(conductorRegistradoVO.getLgcEsContratante()))) {
                    hd.writeCharacters(
                        conductorRegistradoVO.getLgcEsContratante().toString().equalsIgnoreCase("true")
                            ? "SI" : "NO");
                } else {
                    hd.writeCharacters("");
                }
                hd.writeEndElement();
                hd.writeEndElement(); // CONDUCTOR
            }
        } else {
            hd.writeStartElement(CONDUCTOR);
            hd.writeEndElement();
        }
        hd.writeEndElement(); // CONDUCTORES_REGISTRADOS
    }

    private void addBeneficiariosAccidentes(final RespuEmmaPolDto respEmmDto, XMLStreamWriter hd)
        throws XMLStreamException {

        hd.writeStartElement(BENEFICIARIO_ACC);
        if (null != respEmmDto.getVConsBenefAccidentes() && !respEmmDto.getVConsBenefAccidentes().isEmpty()) {
            for (Iterator<BeneficiarioDto> iter = respEmmDto.getVConsBenefAccidentes().iterator(); iter.hasNext();) {
                BeneficiarioDto beneficiarioVO = iter.next();
                hd.writeStartElement(BENEFICIARIO);
                hd.writeStartElement(NUMERO_POLIZA_COND);
                hd.writeCharacters(assingSafeSring(beneficiarioVO.getChrNumPoliza()));
                hd.writeEndElement();
                hd.writeStartElement(FOLIO_CONSECUTIVO);
                hd.writeCharacters(assingSafeSring(beneficiarioVO.getFolioConsecutivo()));
                hd.writeEndElement();
                hd.writeStartElement(NOMBRE);
                hd.writeCharacters(assingSafeSring(beneficiarioVO.getChrBeneficiario()));
                hd.writeEndElement();
                hd.writeStartElement(PORCENTAJE);
                hd.writeCharacters(assingSafeSring(beneficiarioVO.getIntPorcentaje()));
                hd.writeEndElement();
                hd.writeEndElement(); // BENEFICIARIO
            }
        } else {
            hd.writeStartElement(BENEFICIARIO);
            hd.writeEndElement();
        }
        hd.writeEndElement(); // BENEFICIARIO_ACC
    }

    private void addEquipoEspecial(XMLStreamWriter hd, EquipEspDto equipoEspecialVO)
        throws XMLStreamException {

        hd.writeStartElement(EQUIPO_ESPECIAL);
        hd.writeStartElement(NUMERO_POLIZA_COND);
        hd.writeCharacters(assingSafeSring(equipoEspecialVO.getStrNumPoliza()));
        hd.writeEndElement();
        hd.writeStartElement(NUMERO_ENDOSO);
        hd.writeCharacters(assingSafeSring(equipoEspecialVO.getStrNumEndoso()));
        hd.writeEndElement();
        hd.writeStartElement(CLAVE_ESPEFICICACION);
        hd.writeCharacters(assingSafeSring(equipoEspecialVO.getStrCveEspecificacion()));
        hd.writeEndElement();
        hd.writeStartElement(DESC_ESPECIFICACION);
        hd.writeCharacters(assingSafeSring(equipoEspecialVO.getStrDescripcionEsp()));
        hd.writeEndElement();
        hd.writeStartElement(SUMAASEGURADA);
        hd.writeCharacters(assingSafeSring(equipoEspecialVO.getIntSumaAsegurada()));
        hd.writeEndElement();
        hd.writeEndElement(); // EQUIPO_ESPECIAL
    }

    private void addBeneficiarioPreferente(final RespuEmmaPolDto respEmmDto, XMLStreamWriter hd)
        throws XMLStreamException {

        if (null != respEmmDto.getVConsBeneficiario() && !respEmmDto.getVConsBeneficiario().isEmpty()) {
            Iterator<BeneficiarioDto> iterBeneficiario = respEmmDto.getVConsBeneficiario().iterator();
            while (iterBeneficiario.hasNext()) {
                BeneficiarioDto beneficiarioValueObject = iterBeneficiario.next();
                hd.writeStartElement(BENEFICIARIO_PREFERENTE);
                hd.writeStartElement(NUMEROPOLIZA);
                hd.writeCharacters(assingSafeSring(beneficiarioValueObject.getChrNumPoliza()));
                hd.writeEndElement();
                hd.writeStartElement(NOMBRE);
                hd.writeCharacters(assingSafeSring(beneficiarioValueObject.getChrBeneficiario()));
                hd.writeEndElement();
                hd.writeEndElement(); // BENEFICIARIO_PREFERENTE
            }
        } else {
            hd.writeStartElement(BENEFICIARIO_PREFERENTE);
            hd.writeEndElement();
        }
    }

    private void addEndosos(final RespuEmmaPolDto respEmmDto, XMLStreamWriter hd)
        throws XMLStreamException {

        hd.writeStartElement(ENDOSOS);
        if (null != respEmmDto.getVConsEndosos() && !respEmmDto.getVConsEndosos().isEmpty()) {
            Iterator<EndososSOAPDto> iterEndosos = respEmmDto.getVConsEndosos().iterator();
            while (iterEndosos.hasNext()) {
                EndososSOAPDto endososValueObject = iterEndosos.next();
                hd.writeStartElement(ENDOSO);
                hd.writeStartElement(NUMEROPOLIZA);
                hd.writeCharacters(assingSafeSring(endososValueObject.getChrPoliza()));
                hd.writeEndElement();
                hd.writeStartElement(NUMERO_ENDOSO);
                hd.writeCharacters(assingSafeSring(endososValueObject.getChrEndoso()));
                hd.writeEndElement();
                hd.writeStartElement(DESCRIPCION);
                hd.writeCharacters(assingSafeSring(endososValueObject.getChrDescEndoso()));
                hd.writeEndElement();
                hd.writeStartElement(FECHATRABAJO);
                if (endososValueObject.getDteTrabajo() != null) {
                    hd.writeCharacters(UtilServiceDate.formatDate(endososValueObject.getDteTrabajo()));
                } else {
                    hd.writeCharacters("");
                }
                hd.writeEndElement();
                hd.writeStartElement(FECHADESDE);
                if (endososValueObject.getDteDesde() != null) {
                    hd.writeCharacters(UtilServiceDate.formatDate(endososValueObject.getDteDesde()));
                } else {
                    hd.writeCharacters("");
                }
                hd.writeEndElement();
                hd.writeStartElement(FECHAHASTA);
                if (endososValueObject.getDteHasta() != null) {
                    hd.writeCharacters(UtilServiceDate.formatDate(endososValueObject.getDteHasta()));
                } else {
                    hd.writeCharacters("");
                }
                hd.writeEndElement();
                hd.writeStartElement(TARIFA);
                hd.writeCharacters(assingSafeSring(endososValueObject.getChrTarifa()));
                hd.writeEndElement();
                hd.writeStartElement(PRIMANETA);
                hd.writeCharacters(assingSafeSring(endososValueObject.getDcmPrimaNeta()));
                hd.writeEndElement();
                hd.writeStartElement(RECARGO);
                hd.writeCharacters(assingSafeSring(endososValueObject.getDcmRecargo()));
                hd.writeEndElement();
                hd.writeStartElement(DERECHO);
                hd.writeCharacters(assingSafeSring(endososValueObject.getDcmDerecho()));
                hd.writeEndElement();
                hd.writeStartElement(IMPUESTO);
                hd.writeCharacters(assingSafeSring(endososValueObject.getDcmImpuesto()));
                hd.writeEndElement();
                hd.writeStartElement(PRIMATOTAL);
                hd.writeCharacters(assingSafeSring(endososValueObject.getDcmPrimaTotal()));
                hd.writeEndElement();

                addCoberturas(respEmmDto, hd, endososValueObject.getChrEndoso());
                addRecibos(respEmmDto, hd, endososValueObject.getChrEndoso());
                addMovimientos(respEmmDto, hd, endososValueObject.getChrEndoso());
                addEquiposEspeciales(respEmmDto, hd, endososValueObject.getChrEndoso());

                hd.writeEndElement(); // ENDOSO
            }

        } else {
            hd.writeStartElement(ENDOSO);
            addCoberturas(respEmmDto, hd, null);
            addRecibos(respEmmDto, hd, null);
            addMovimientos(respEmmDto, hd, null);
            addEquiposEspeciales(respEmmDto, hd, null);
            hd.writeEndElement(); // ENDOSO
        }

        hd.writeEndElement(); // ENDOSOS
    }

    private void addCoberturas(final RespuEmmaPolDto respEmmDto, XMLStreamWriter hd, String endoso)
        throws XMLStreamException {

        hd.writeStartElement(COBERTURAS);
        if (null != respEmmDto.getVConsCoberturas() && !respEmmDto.getVConsCoberturas().isEmpty()) {
            Iterator<CoberturasDto> iterCoberturas = respEmmDto.getVConsCoberturas().iterator();
            if (endoso != null && !endoso.equals("")) {
                // FILTRADOS POR ENDOSO
                while (iterCoberturas.hasNext()) {
                    CoberturasDto coberturasValueObject = iterCoberturas.next();

                    if (coberturasValueObject.getChrEndoso().equals(endoso)) {
                        addCobertura(hd, coberturasValueObject);
                    }
                }
            } else {
                // TODOS
                while (iterCoberturas.hasNext()) {
                    CoberturasDto coberturasValueObject = iterCoberturas.next();

                    addCobertura(hd, coberturasValueObject);
                }
            }
        } else {
            hd.writeStartElement(COBERTURA);
            hd.writeEndElement();
        }

        hd.writeEndElement(); // COBERTURAS
    }

    private void addCobertura(XMLStreamWriter hd, CoberturasDto coberturasValueObject)
        throws XMLStreamException {

        hd.writeStartElement(COBERTURA);

        hd.writeStartElement(NUMEROPOLIZA);
        hd.writeCharacters(assingSafeSring(coberturasValueObject.getChrPoliza()));
        hd.writeEndElement();
        hd.writeStartElement(NUMERO_ENDOSO);
        hd.writeCharacters(assingSafeSring(coberturasValueObject.getChrEndoso()));
        hd.writeEndElement();
        hd.writeStartElement(DESCRIPCION);
        hd.writeCharacters(assingSafeSring(coberturasValueObject.getChrCobertura()));
        hd.writeEndElement();
        hd.writeStartElement(SUMA);
        hd.writeCharacters(assingSafeSring(coberturasValueObject.getChrSuma()).trim());
        hd.writeEndElement();
        hd.writeStartElement(DEDUCIBLE);
        hd.writeCharacters(assingSafeSring(coberturasValueObject.getChrDed()));
        hd.writeEndElement();
        hd.writeStartElement(DEDUCIBLE_CALCULADO);
        hd.writeCharacters(assingSafeSring(coberturasValueObject.getChrDedCalc()));
        hd.writeEndElement();
        hd.writeStartElement(PRIMATOTAL);
        hd.writeCharacters(assingSafeSring(coberturasValueObject.getChrPrima()).trim());
        hd.writeEndElement();

        hd.writeEndElement(); // COBERTURA
    }

    private void addRecibos(final RespuEmmaPolDto respEmmDto, XMLStreamWriter hd, String endoso)
        throws XMLStreamException {

        hd.writeStartElement(RECIBOS);
        if (null != respEmmDto.getVConsRecibos() && !respEmmDto.getVConsRecibos().isEmpty()) {
            Iterator<RecibosDto> iterRecibos = respEmmDto.getVConsRecibos().iterator();
            if (endoso != null && !endoso.equals("")) {
                // FILTRADOS POR ENDOSO
                while (iterRecibos.hasNext()) {
                    RecibosDto recibosValueObject = iterRecibos.next();

                    if (recibosValueObject.getChrEndoso().equals(endoso)) {
                        addRecibo(hd, recibosValueObject);
                    }
                }
            } else {
                // TODOS
                while (iterRecibos.hasNext()) {
                    RecibosDto recibosValueObject = iterRecibos.next();

                    addRecibo(hd, recibosValueObject);
                }
            }
        } else {
            hd.writeStartElement(RECIBO);
            hd.writeEndElement();
        }

        hd.writeEndElement(); // RECIBOS
    }

    private void addRecibo(XMLStreamWriter hd, RecibosDto recibosValueObject)
        throws XMLStreamException {

        hd.writeStartElement(RECIBO);

        hd.writeStartElement(NUMEROPOLIZA);
        hd.writeCharacters(assingSafeSring(recibosValueObject.getChrPoliza()));
        hd.writeEndElement();
        hd.writeStartElement(NUMERO_ENDOSO);
        hd.writeCharacters(assingSafeSring(recibosValueObject.getChrEndoso()));
        hd.writeEndElement();
        hd.writeStartElement(FECHATRABAJO);
        if (recibosValueObject.getDteTrabajo() != null) {
            hd.writeCharacters(UtilServiceDate.formatDate(recibosValueObject.getDteTrabajo()));
        } else {
            hd.writeCharacters("");
        }
        hd.writeEndElement();
        hd.writeStartElement(FECHADESDE);
        if (recibosValueObject.getDteDesde() != null) {
            hd.writeCharacters(UtilServiceDate.formatDate(recibosValueObject.getDteDesde()));
        } else {
            hd.writeCharacters("");
        }
        hd.writeEndElement();
        hd.writeStartElement(FECHAHASTA);
        if (recibosValueObject.getDteHasta() != null) {
            hd.writeCharacters(UtilServiceDate.formatDate(recibosValueObject.getDteHasta()));
        } else {
            hd.writeCharacters("");
        }
        hd.writeEndElement();
        hd.writeStartElement(COMPROBANTE_PAGO);
        hd.writeCharacters(assingSafeSring(recibosValueObject.getChrComprobPago()));
        hd.writeEndElement();
        // JLSM
        hd.writeStartElement(MED_PAG);
        hd.writeCharacters(assingSafeSring(recibosValueObject.getChrMedPag()));
        hd.writeEndElement();
        // JLSM
        hd.writeStartElement(FECHA_PAGO);
        if (recibosValueObject.getDtePago() != null) {
            hd.writeCharacters(UtilServiceDate.formatDate(recibosValueObject.getDtePago()));
        } else {
            hd.writeCharacters("");
        }
        hd.writeEndElement();
        hd.writeStartElement(FECHA_IMPRESION);
        if (recibosValueObject.getDteImpresion() != null) {
            hd.writeCharacters(UtilServiceDate.formatDate(recibosValueObject.getDteImpresion()));
        } else {
            hd.writeCharacters("");
        }
        hd.writeEndElement();
        hd.writeStartElement(SERIE_RECIBO);
        hd.writeCharacters(assingSafeSring(recibosValueObject.getIntSerieRecibo()));
        hd.writeEndElement();
        hd.writeStartElement(ESTADO_RECIBO);
        hd.writeCharacters(assingSafeSring(recibosValueObject.getChrEdoRecibo()));
        hd.writeEndElement();
        hd.writeStartElement(FOLIO_RECIBO);
        hd.writeCharacters(assingSafeSring(recibosValueObject.getChrFolio()));
        hd.writeEndElement();
        hd.writeStartElement(PRIMANETA);
        hd.writeCharacters(assingSafeSring(recibosValueObject.getDcmPrimaNeta()));
        hd.writeEndElement();
        hd.writeStartElement(RECARGO);
        hd.writeCharacters(assingSafeSring(recibosValueObject.getDcmRecargo()));
        hd.writeEndElement();
        hd.writeStartElement(DERECHO);
        hd.writeCharacters(assingSafeSring(recibosValueObject.getDcmDerecho()));
        hd.writeEndElement();
        hd.writeStartElement(IMPUESTO);
        hd.writeCharacters(assingSafeSring(recibosValueObject.getDcmImpuesto()));
        hd.writeEndElement();
        hd.writeStartElement(PRIMATOTAL);
        hd.writeCharacters(assingSafeSring(recibosValueObject.getDcmPrimaTotal()));
        hd.writeEndElement();

        hd.writeEndElement(); // RECIBO
    }

    private void addEquiposEspeciales(final RespuEmmaPolDto respEmmDto, XMLStreamWriter hd, String endoso)
        throws XMLStreamException {

        hd.writeStartElement(EQUIPO_ESP_ADAP);
        if (null != respEmmDto.getVConsEquipoEspecial() && !respEmmDto.getVConsEquipoEspecial().isEmpty()) {
            Iterator<EquipEspDto> iterEquipoEspecial = respEmmDto.getVConsEquipoEspecial().iterator();
            if (endoso != null && !endoso.equals("")) {
                while (iterEquipoEspecial.hasNext()) {
                    EquipEspDto equipoEspecialVO = iterEquipoEspecial.next();
                    if (equipoEspecialVO.getStrNumEndoso().equals(endoso)) {
                        addEquipoEspecial(hd, equipoEspecialVO);
                    }
                }
            } else {
                while (iterEquipoEspecial.hasNext()) {
                    EquipEspDto equipoEspecialVO = iterEquipoEspecial.next();
                    addEquipoEspecial(hd, equipoEspecialVO);
                }
            }
        } else {
            hd.writeStartElement(EQUIPO_ESPECIAL);
            hd.writeEndElement();
        }
        hd.writeEndElement(); // EQUIPO_ESP_ADAP
    }

    private void addMovimientos(final RespuEmmaPolDto respEmmDto, XMLStreamWriter hd, String endoso)
        throws XMLStreamException {

        hd.writeStartElement(MOVIMIENTOS);
        if (null != respEmmDto.getVConsEndososB() && !respEmmDto.getVConsEndososB().isEmpty()) {
            Iterator<EndososBDto> iterEndososB = respEmmDto.getVConsEndososB().iterator();
            if (endoso != null && !endoso.equals("")) {
                while (iterEndososB.hasNext()) {
                    EndososBDto endososBValueObject = iterEndososB.next();
                    if (endososBValueObject.getChrEndoso().equals(endoso)) {
                        addMovimiento(hd, endososBValueObject);
                    }
                }
            } else {
                while (iterEndososB.hasNext()) {
                    EndososBDto endososBValueObject = iterEndososB.next();
                    addMovimiento(hd, endososBValueObject);
                }
            }
        } else {
            hd.writeStartElement(MOVIMIENTO);
            hd.writeEndElement();
        }

        hd.writeEndElement(); // MOVIMIENTOS
    }

    private void addMovimiento(XMLStreamWriter hd, EndososBDto endososBValueObject)
        throws XMLStreamException {

        hd.writeStartElement(MOVIMIENTO);
        hd.writeStartElement(NUMEROPOLIZA);
        hd.writeCharacters(assingSafeSring(endososBValueObject.getChrPoliza()));
        hd.writeEndElement();
        hd.writeStartElement(NUMERO_ENDOSO);
        hd.writeCharacters(assingSafeSring(endososBValueObject.getChrEndoso()));
        hd.writeEndElement();
        hd.writeStartElement(DESCRIPCION);
        hd.writeCharacters(assingSafeSring(endososBValueObject.getChrDescEndoso()));
        hd.writeEndElement();
        hd.writeStartElement(DATO_ANTERIOR);
        hd.writeCharacters(assingSafeSring(endososBValueObject.getChrDatoAnterior()));
        hd.writeEndElement();
        hd.writeStartElement(DATO_ACTUAL);
        hd.writeCharacters(assingSafeSring(endososBValueObject.getChrDatoActual()));
        hd.writeEndElement();
        hd.writeEndElement(); // MOVIMIENTO
    }

    private static String assingSafeSring(Object value) {
        if (value == null) {
            return "";
        }
        return value.toString();
    }
}
